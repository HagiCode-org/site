---
import type { AboutPageModel } from '@/lib/about-page-model';
import AboutSnapshotRuntime from './AboutSnapshotRuntime';
import './about-snapshot-page.css';

interface Props {
  model: AboutPageModel;
}

const { model } = Astro.props;
---

<AboutSnapshotRuntime model={model} client:load />
