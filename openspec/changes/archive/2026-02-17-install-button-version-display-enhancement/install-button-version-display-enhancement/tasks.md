# 实施任务清单

## 概述

本文档列出了优化 InstallButton 组件版本显示的具体实施任务。

**核心目标**：
- 增强操作系统版本区分
- 优化显示名称格式（包含版本号、架构）
- 扩大下拉面板尺寸
- 确保两个站点实现一致

## 任务列表

### 阶段 1：准备工作

- [ ] **1.1 验证当前实现**
  - 检查文档站点 InstallButton 组件（`apps/docs/src/components/InstallButton.tsx`）
  - 检查营销站点 InstallButton 组件（`apps/website/src/components/home/InstallButton.tsx`）
  - 检查共享类型定义（`packages/shared/src/types/desktop.ts`）
  - 检查工具函数（`packages/shared/src/desktop-utils.ts`）
  - **预期结果**：了解当前实现细节和依赖关系
  - **验证方式**：代码审查

- [ ] **1.2 准备开发环境**
  - 确保本地开发服务器可以正常运行（`npm run dev`）
  - 验证可以访问两个站点的 InstallButton 组件
  - 测试构建命令是否正常（`npm run build`）
  - **预期结果**：开发环境准备就绪
  - **验证方式**：本地测试

- [ ] **1.3 确认设计规范**
  - 确认面板尺寸目标值（宽度 380px，最大高度 500px）
  - 确认显示格式（版本号 + 架构 + 文件大小）
  - 确认平台图标（🍎 macOS、🪟 Windows、🐧 Linux）
  - **预期结果**：设计规范明确
  - **验证方式**：参考 proposal.md

### 阶段 2：更新共享层

- [ ] **2.1 更新类型定义（可选）**
  - 检查 `PlatformDownload` 接口是否需要添加字段
  - 评估是否需要添加版本号字段到接口中
  - 如果需要，更新 `packages/shared/src/types/desktop.ts`
  - **预期结果**：类型定义支持所需功能
  - **验证方式**：类型检查通过

- [ ] **2.2 更新工具函数**
  - 修改 `getAssetTypeLabel` 函数（`packages/shared/src/desktop-utils.ts`）
  - 添加 `formatAssetLabel` 函数，格式化完整的标签
  - 实现版本号和架构信息的组合逻辑
  - **预期结果**：工具函数支持详细显示格式
  - **验证方式**：单元测试或手动测试

```typescript
// 示例实现
export function formatAssetLabel(
  assetType: AssetType,
  version?: string,
  includeArchitecture?: boolean
): string {
  const typeLabel = getAssetTypeLabel(assetType);
  const versionStr = version ? ` - ${version}` : '';
  return `${typeLabel}${versionStr}`;
}

export function getArchitectureLabel(assetType: AssetType): string {
  const archLabels: Record<AssetType, string> = {
    [AssetType.MacOSApple]: 'ARM64',
    [AssetType.MacOSIntel]: 'x64',
    [AssetType.WindowsSetup]: 'x64',
    [AssetType.WindowsPortable]: 'x64',
    [AssetType.LinuxAppImage]: '通用',
    [AssetType.LinuxDeb]: 'amd64',
    [AssetType.LinuxTarball]: '通用',
    // ...
  };
  return archLabels[assetType] || '';
}
```

- [ ] **2.3 添加平台图标常量**
  - 在 `packages/shared/src/desktop-utils.ts` 中添加平台图标映射
  - **预期结果**：可以方便地获取平台图标
  - **验证方式**：代码审查

```typescript
export const PLATFORM_ICONS: Record<string, string> = {
  macos: '🍎',
  windows: '🪟',
  linux: '🐧',
};
```

### 阶段 3：更新营销站点 InstallButton

- [ ] **3.1 更新组件逻辑**
  - 修改 `apps/website/src/components/home/InstallButton.tsx`
  - 更新下拉面板渲染逻辑，使用新的格式化函数
  - 添加版本号到平台分组标题
  - **预期结果**：组件显示更详细的信息
  - **验证方式**：本地开发测试

- [ ] **3.2 更新下拉面板样式**
  - 修改 `apps/website/src/components/home/InstallButton.module.css`
  - 调整面板宽度：280px → 380px
  - 调整面板最大高度：400px → 500px
  - 调整行高：32px → 40px
  - 调整内边距：8px → 12px
  - **预期结果**：面板尺寸符合设计规范
  - **验证方式**：视觉检查

```css
/* InstallButton.module.css */
.dropdownMenu {
  width: 380px; /* 从 280px 增加 */
  max-height: 500px; /* 从 400px 增加 */
}

.dropdownItem {
  min-height: 40px; /* 从 32px 增加 */
  padding: 12px; /* 从 8px 增加 */
}
```

- [ ] **3.3 添加架构信息显示**
  - 在每个下载选项中添加架构标签
  - 使用徽章样式显示架构类型（如 ARM64、x64）
  - **预期结果**：架构信息清晰可见
  - **验证方式**：视觉检查

- [ ] **3.4 添加平台图标**
  - 在平台分组标题中添加对应图标
  - **预期结果**：增强平台识别性
  - **验证方式**：视觉检查

- [ ] **3.5 添加推荐标识**
  - 为推荐的下载选项添加星标或"推荐"标签
  - **预期结果**：推荐版本有明确标识
  - **验证方式**：视觉检查

### 阶段 4：更新文档站点 InstallButton

- [ ] **4.1 创建或更新样式文件**
  - 检查文档站点是否已有独立的 InstallButton 样式文件
  - 如果没有，创建 `apps/docs/src/styles/install-button.css`
  - **预期结果**：有独立的样式文件
  - **验证方式**：文件存在

- [ ] **4.2 更新组件逻辑**
  - 修改 `apps/docs/src/components/InstallButton.tsx`
  - 应用与营销站点相同的逻辑变更
  - **预期结果**：两个站点实现一致
  - **验证方式**：代码审查和对比

- [ ] **4.3 更新下拉面板样式**
  - 更新样式文件，应用与营销站点相同的尺寸调整
  - **预期结果**：面板尺寸一致
  - **验证方式**：视觉检查

- [ ] **4.4 确保样式一致性**
  - 对比两个站点的样式，确保视觉效果一致
  - **预期结果**：用户在不同站点看到相同的下拉面板
  - **验证方式**：并排对比测试

### 阶段 5：优化和测试

- [ ] **5.1 添加响应式样式**
  - 为移动设备添加响应式断点
  - 在小屏幕上调整面板宽度（使用 100% 或较小宽度）
  - **预期结果**：移动端显示正常
  - **验证方式**：浏览器响应式测试

```css
@media (max-width: 480px) {
  .dropdownMenu {
    width: 100%;
    max-width: 350px;
  }
}
```

- [ ] **5.2 本地功能测试**
  - 测试营销站点 InstallButton：
    - ✅ 版本号正确显示
    - ✅ 架构信息正确显示
    - ✅ 平台图标正确显示
    - ✅ 推荐标识正确显示
    - ✅ 下载功能正常
  - 测试文档站点 InstallButton：
    - ✅ 所有上述功能正常
  - **预期结果**：所有功能正常工作
  - **验证方式**：手动测试

- [ ] **5.3 跨浏览器测试**
  - 在以下浏览器中测试：
    - Chrome/Edge (Chromium)
    - Firefox
    - Safari (如可用)
  - **预期结果**：所有主流浏览器功能一致
  - **验证方式**：逐一测试各浏览器

- [ ] **5.4 响应式测试**
  - 测试不同屏幕尺寸：
    - 桌面 (>1024px)
    - 平板 (768px-1024px)
    - 手机 (<768px)
  - **预期结果**：各尺寸下显示正常
  - **验证方式**：浏览器响应式设计模式

- [ ] **5.5 类型检查测试**
  - 运行 TypeScript 类型检查
  - 确保没有类型错误
  - **预期结果**：类型检查通过
  - **验证方式**：`npm run typecheck`

- [ ] **5.6 构建测试**
  - 运行 `npm run build` 确保构建成功
  - 检查构建输出是否有警告或错误
  - **预期结果**：构建成功，无警告
  - **验证方式**：构建命令

### 阶段 6：文档和清理

- [ ] **6.1 更新组件文档**
  - 为 InstallButton 添加 JSDoc 注释
  - 说明新的显示格式
  - 更新相关 TypeScript 类型定义
  - **预期结果**：代码有良好的文档注释

- [ ] **6.2 清理未使用的代码**
  - 检查是否有未使用的导入或变量
  - 清理任何临时调试代码
  - **预期结果**：代码库整洁，无死代码

- [ ] **6.3 添加变更注释**
  - 在相关文件中添加注释说明变更原因
  - **预期结果**：代码可维护性提升

### 阶段 7：部署验证

- [ ] **7.1 创建 Pull Request**
  - 提交变更到新分支
  - 创建 PR，描述变更内容
  - **预期结果**：PR 创建成功

- [ ] **7.2 部署到预览环境**
  - 通过 CI/CD 部署预览版本
  - 在预览环境中验证功能
  - **预期结果**：预览环境功能正常

- [ ] **7.3 合并到主分支**
  - 代码审查通过后合并
  - 触发生产环境部署
  - **预期结果**：生产环境更新成功

- [ ] **7.4 生产环境验证**
  - 访问生产环境的两个站点
  - 验证版本信息正确显示
  - 验证两个站点实现一致
  - **预期结果**：生产环境功能正常

## 实施注意事项

### 开发原则

1. **保持一致性**：两个站点的实现必须保持一致
2. **渐进式实施**：按照任务顺序逐步完成
3. **持续验证**：每完成一个任务立即进行验证
4. **保持兼容**：确保修改不影响现有功能

### 样式约定

- 使用 CSS 变量定义颜色和尺寸
- 确保暗色模式兼容性
- 遵循项目现有的样式约定

### 回滚计划

如果部署后发现严重问题：
1. 回滚到之前的版本
2. 下拉面板恢复到原来的显示格式
3. 不影响版本数据获取逻辑
4. 不影响其他组件功能

### 已知限制

1. **版本号来源**：当前版本号来自构建时获取的数据，需要版本更新后才会显示新版本号
2. **架构推断**：架构信息从文件名推断，如果文件命名不规范可能显示不准确
3. **移动端空间**：小屏幕设备可能需要进一步优化显示

### 预计工时

- 阶段 1：1 小时
- 阶段 2：2-3 小时
- 阶段 3：2-3 小时
- 阶段 4：1-2 小时
- 阶段 5：2-3 小时
- 阶段 6：0.5 小时
- 阶段 7：1 小时

**总计**：约 10-14 小时
