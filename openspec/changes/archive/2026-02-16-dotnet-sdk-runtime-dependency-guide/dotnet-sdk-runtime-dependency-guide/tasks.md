# 简化 .NET SDK 安装文档任务清单

## 1. 更新文档元数据

- [ ] 1.1 更新 `title` 字段，移除"开发环境"相关描述
- [ ] 1.2 更新 `description` 字段，从"开发环境安装指南"改为"运行依赖安装指南"

## 2. 简化引言部分

- [ ] 2.1 删除"什么是 .NET SDK"章节
- [ ] 2.2 添加简短的"简要说明"章节，说明 .NET SDK 是 Hagicode 运行依赖
- [ ] 2.3 更新 Tip 内容，简化版本要求描述

## 3. 删除开发相关内容

- [ ] 3.1 删除 `dotnet new console` 命令示例
- [ ] 3.2 删除 `dotnet build` 命令示例
- [ ] 3.3 删除 `dotnet run` 命令示例
- [ ] 3.4 删除 CLI 工具、编译器、项目模板的技术介绍
- [ ] 3.5 删除"使用 .NET SDK，您可以："命令列表

## 4. 删除版本管理章节

- [ ] 4.1 删除"版本管理"章节标题
- [ ] 4.2 删除"使用 global.json 固定项目版本"内容
- [ ] 4.3 删除"列出已安装的 SDK 版本"（`dotnet --list-sdks`）
- [ ] 4.4 删除"列出已安装的运行时版本"（`dotnet --list-runtimes`）

## 5. 简化验证安装章节

- [ ] 5.1 保留 `dotnet --version` 命令
- [ ] 5.2 保留 `dotnet --info` 命令
- [ ] 5.3 删除"验证命令输出示例"章节
- [ ] 5.4 删除"创建测试项目"章节及其所有内容（`dotnet new console -o testapp` 等）

## 6. 简化故障排除章节

- [ ] 6.1 精简 Windows 问题：保留"命令不可用"，删除"版本过旧"和"网络问题"
- [ ] 6.2 精简 macOS 问题：保留"命令不可用"，删除"架构问题"
- [ ] 6.3 精简 Linux 问题：保留"权限错误"，删除"版本过旧"和"缺少依赖"
- [ ] 6.4 删除"检查环境"章节（详细的 `which dotnet`、`Get-Command dotnet` 等）

## 7. 简化中国大陆用户加速章节

- [ ] 7.1 简化为单段说明："如果下载速度慢，可使用 Azure 中国镜像"
- [ ] 7.2 删除详细的 NuGet.Config 手动编辑步骤
- [ ] 7.3 删除"使用国内镜像源"中的详细 XML 配置示例
- [ ] 7.4 删除"下载 .NET SDK 加速"的 Tabs 组件，保留镜像链接列表

## 8. 调整卸载章节位置

- [ ] 8.1 将"卸载 .NET SDK"章节标题改为"附录：卸载 .NET SDK"
- [ ] 8.2 确保卸载方法保留完整内容（不影响用户卸载）

## 9. 更新导航和链接

- [ ] 9.1 删除"下一步"章节中的"返回安装指南"链接
- [ ] 9.2 更新"了解更多"链接，添加指向 .NET 官方文档的链接
- [ ] 9.3 删除"使用 .NET CLI 创建项目"链接
- [ ] 9.4 检查并更新文档中的内部链接

## 10. 验证和测试

- [ ] 10.1 运行 `npm run build` 确认构建成功
- [ ] 10.2 运行 `npm run typecheck` 确认无类型错误
- [ ] 10.3 启动开发服务器 `npm run dev` 验证页面显示正常
- [ ] 10.4 检查所有链接是否有效（无 404）
- [ ] 10.5 验证 Tabs 组件正常工作
- [ ] 10.6 确认文档长度显著减少（目标：约 300-400 行）

## 11. 代码审查

- [ ] 11.1 创建 Pull Request
- [ ] 11.2 等待代码审查
- [ ] 11.3 根据反馈进行修改
- [ ] 11.4 合并到主分支

## 12. 归档变更（部署后）

- [ ] 12.1 将变更目录移动到 `openspec/changes/archive/2026-02-16-dotnet-sdk-runtime-dependency-guide/`
- [ ] 12.2 运行 `openspec validate --strict` 确认归档正确
